<UserControl x:Class="DeepCode.Views.SessionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:DeepCode.Models"
             xmlns:converters="clr-namespace:DeepCode.Views.Converters"
             d:DataContext="{d:DesignInstance Type=models:ChatViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:StatusBadgeConverter x:Key="StatusBadge" />
        <converters:TimeAgoConverter x:Key="TimeAgo" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12,8" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="关闭"
                        Command="{Binding ToggleSessionListCommand}"
                        Background="Transparent" BorderBrush="Transparent" />
                <Button DockPanel.Dock="Right" Content="新建"
                        Command="{Binding CreateNewSessionCommand}"
                        Background="Transparent" BorderBrush="Transparent" Margin="0,0,8,0" />
                <TextBlock Text="会话历史" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
            </DockPanel>
        </Border>

        <ListBox Grid.Row="1" ItemsSource="{Binding SessionList}"
                 SelectionMode="Single" SelectionChanged="OnSessionSelected"
                 BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="2">
                        <TextBlock Text="{Binding Summary}" FontSize="12"
                                   TextWrapping="Wrap" MaxHeight="36" TextTrimming="CharacterEllipsis" />
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="{Binding Status, Converter={StaticResource StatusBadge}}"
                                       FontSize="11" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding UpdateTime, Converter={StaticResource TimeAgo}}"
                                       FontSize="11" Foreground="#888888" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
